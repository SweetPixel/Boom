var MainMenu				: GameObject;private var curAction 		: int = 0;private var WindowArea		: Rect = Rect(50,50,400,200);private var validTime		: float;function OnGUI () {	if (curAction > 0)	{		if (Time.time >= validTime)		{			if (curAction == 1)			{				Debug.Log("In a real game, you would simply destroy this prefab and have another way of instantiating it at runtime");				Debug.Log("For now, I will just start it again manually");				gameObject.SendMessage("ActivateMenu", true);				gameObject.SendMessage("TraverseMenu");				curAction = 0;			} else			{				if (MainMenu)				{					curAction = 0;					Instantiate(MainMenu);					Destroy(gameObject);				}			}		}	}}function Start(){	gameObject.SendMessage("TraverseMenu");}function OnResume(){	validTime = Time.time + 5;	curAction = 1;}function OnQuitGame(){	validTime = Time.time + 2;	curAction = 2;}